- name: use loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: use loops here
    ansible.builtin.debug:
      msg: "Hello {{ item }}"
    loop:
    - suresh 
    - ramesh 
    - john 
    - rahim 