- name: use Conditions
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: using Conditions
    ansible.builtin.package:
      name: nginx
      state: latest
    when: 
    - ansible_os_family == "RedHat"
    - ansible_pkg_mgr == "apt"